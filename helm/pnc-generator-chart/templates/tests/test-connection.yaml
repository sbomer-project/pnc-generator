apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pnc-generator-chart.fullname" . }}-test-connection"
  labels:
    {{- include "pnc-generator-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pnc-generator-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
